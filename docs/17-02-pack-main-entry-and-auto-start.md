# Требования: pack как основной вход, без меню "Создать стикер"

**Дата:** 17.02.2026  
**Статус:** proposed  
**Связано:** [done/16-02-pack-flow-ux.md](done/16-02-pack-flow-ux.md), [16-02-pack-callback-ux-hardening.md](16-02-pack-callback-ux-hardening.md), [17-02-assistant-pack-entry-refactor.md](17-02-assistant-pack-entry-refactor.md)

---

## 1. Что меняем

1. Скрываем в нижнем меню точку входа `✨ Создать стикер` (логику в коде пока не удаляем).
2. Для возвращающегося пользователя автоматически запускаем flow `📦 Создать пак`.
3. Для нового пользователя первый вход в бот также всегда стартует через flow `📦 Создать пак`.

---

## 2. Проблема

Сейчас пользователь видит два параллельных входа (`стикер` и `пак`), что создает лишний выбор и путаницу.

И для нового, и для возвращающегося пользователя логичнее иметь единый entrypoint: `Создать пак`.

---

## 3. Цель

- Сделать `Создать пак` основным пользовательским сценарием.
- Сократить число кликов до первого полезного действия.
- Убрать неоднозначность в меню и входном UX.

---

## 4. Продуктовые правила

### 4.1 Меню

- Кнопка `✨ Создать стикер` скрывается из постоянного меню (без удаления кода/хендлера).
- В меню остается вход `📦 Создать пак` (и остальные не связанные пункты, если есть).

### 4.2 Триггеры входа в pack flow

Pack flow запускается в двух случаях:

1. при `/start`;
2. при нажатии кнопки меню `📦 Создать пак`.

Для обоих триггеров действуют одинаковые guard-правила:

- пользователь не находится в активном processing-состоянии;
- не идет незавершенный конфликтующий flow, который нужно сначала завершить/отменить.

### 4.3 Поведение при активном processing-состоянии

- Если у пользователя уже идет тяжелый процесс (`generating_pack_preview`, `processing_pack`), новый авто-вход не должен сбрасывать сессию.
- Бот показывает короткий статус текущего процесса и не создает новый конкурирующий flow.
- Это правило защищает от "двойных запусков" и потери контекста.

### 4.4 Если фото нет

- Flow `Создать пак` все равно стартует.
- Пользователь попадает на первый шаг pack flow: "Отправь фото для начала".
- Это правило одинаково для нового и returning пользователя.

### 4.5 Deep-link исключения (текущие)

- Deep-link с payload `val_*` имеет приоритет и работает по текущей специальной логике (не заменяется на auto-pack).
- Остальные payload сейчас используются только как источник атрибуции (UTM/канал) и не задают отдельный генерационный сценарий.

### 4.6 Временное решение по assistant на `/start`

- Текущий запуск `startAssistantDialog` из `/start` временно отключается.
- Полная переделка assistant-entry в pack-entry оформляется и реализуется отдельно (см. `17-02-assistant-pack-entry-refactor.md`).

---

## 5. UX-поведение

### 5.1 Возврат с фото

- Бот сразу открывает актуальный шаг pack flow (карусель/выбор набора или восстановление последнего валидного шага).
- Пользователь получает краткое системное сообщение в стиле:
  - RU: "Продолжаем с последним фото. Открываю создание пака."
  - EN: "Continuing with your latest photo. Opening pack flow."

### 5.2 Вход без фото (new или returning)

- Бот автоматически открывает pack flow.
- Показать явный CTA на загрузку фото как первый шаг сценария.

### 5.3 Первый вход нового пользователя

- На первом `/start` сразу открывается pack flow (без отдельного выбора режима).
- Если фото еще нет, сценарий остается внутри pack flow и просит фото.

### 5.4 Идемпотентность

- Автостарт должен быть одноразовым на вход (без циклических переоткрытий при каждом служебном апдейте).

---

## 6. Технические требования

1. Скрыть кнопку `✨ Создать стикер` из `ReplyKeyboard`, но не удалять существующий хендлер/логику.
2. Переключить `/start` на pack-entry (вместо `startAssistantDialog`) с учетом deep-link исключений.
3. Добавить единый guard для "auto-pack entry" (new + returning):
   - проверка конфликтующих состояний;
   - защита от перезапуска при активном processing;
   - запуск безопасного entry в pack flow.
4. Использовать текущее архитектурное решение по рабочему фото: `session.current_photo_file_id || user.last_photo_file_id`.
5. Стратегия сессий — как сейчас: использовать текущую реализацию pack-entry (деактивация старой active session и создание новой pack session), без рефакторинга в этом таске.
6. Не ломать существующие anti-duplicate механики (`session_id`, `session_rev`, reject stale callbacks).
7. Логировать факт автозапуска (`auto_pack_entry=true`) для наблюдаемости.

---

## 7. Риски и ограничения

- Риск: автостарт перехватит пользователя в момент, когда он ожидал другой flow.  
  Митигация: строгий guard по состояниям + fallback в нейтральный экран.

- Риск: повторный автостарт на одном входе создаст ощущение "бот зациклился".  
  Митигация: одноразовый флаг автостарта на событие входа.

---

## 8. Тест-план (smoke)

1. Новый пользователь (первый `/start`, фото еще нет):
   - сразу открывается pack flow;
   - бот запрашивает фото в рамках pack flow.
2. Возвращающийся пользователь с `last_photo_file_id` при `/start`:
   - бот автоматически открывает pack flow.
3. Возвращающийся пользователь с `last_photo_file_id` при нажатии `📦 Создать пак`:
   - бот открывает pack flow через тот же entrypoint.
4. Возвращающийся пользователь без фото:
   - pack flow стартует и показывает шаг загрузки фото.
5. Deep-link `val_*`:
   - срабатывает текущий специальный сценарий, auto-pack не перехватывает.
6. Пользователь в активном processing-состоянии:
   - новый вход не ломает текущий процесс, показывается корректный текущий статус.
7. Проверка меню:
   - кнопки `✨ Создать стикер` больше нет.
8. Проверка логов:
   - есть события автозапуска и нет всплеска `wrong_state/stale_callback`.

---

## 9. Критерии приемки

1. В пользовательском меню отсутствует вход `Создать стикер`.
2. Новый пользователь при первом входе автоматически попадает в pack flow.
3. Возвращающийся пользователь с фото при входе автоматически попадает в pack flow.
4. Возвращающийся пользователь по кнопке `📦 Создать пак` попадает в тот же pack-entry.
5. Deep-link `val_*` продолжает работать без регрессии.
6. Пользователь без фото не попадает в "битый" сценарий и получает понятный CTA внутри pack flow.
7. Нет регрессий в активных pack/session-state сценариях.

---

## 10. Checklist реализации

- [ ] Скрыть кнопку `✨ Создать стикер`, не удаляя текущую логику/хендлер.
- [ ] Переключить `/start` с `startAssistantDialog` на pack-entry с deep-link исключениями.
- [ ] Добавить guard + entrypoint автозапуска pack для new и returning пользователей.
- [ ] Добавить одноразовую защиту от повторного автостарта на одном входе.
- [ ] Добавить логирование `auto_pack_entry`.
- [ ] Прогнать smoke-кейсы на test.
- [ ] Обновить архитектурную документацию при изменении API flow (`docs/architecture/01-api-bot.md`).

