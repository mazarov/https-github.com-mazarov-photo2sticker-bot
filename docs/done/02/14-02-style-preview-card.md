# Style Preview — превью стиля перед применением

> Дата: 14.02.2026
> Статус: IN PROGRESS (ветка `feature/style-preview-card`)

## Проблема

При выборе стиля пользователь видит только эмодзи + название. Непонятно, как будет выглядеть результат. Это приводит к трате кредитов "вслепую". Поведение должно быть единым во всех местах, где выбирается стиль.

## Места выбора стиля (unified logic)

| Место | Когда показывается | Текущий callback |
|-------|-------------------|------------------|
| **Меню идей** — кнопка "🔄 Другой стиль" | После загрузки фото, в карточке идеи | `asst_idea_restyle:{styleId}:{ideaIndex}` |
| **Manual mode** — `sendStyleKeyboardFlat` | При ручном выборе стиля (wait_style) | `style_v2:{styleId}` |
| **Carousel** — `sendStyleCarousel` | При ручном выборе стиля (wait_style) | `style_carousel_pick:{styleId}` |

**Все три места должны работать одинаково:** клик на стиль → превью (стикер + описание + ОК) → применить.

## Текущий флоу

```
Меню идей:
  [🔄 Другой стиль] → список стилей → клик → сразу применяется

Manual mode:
  [Список стилей] → клик → сразу генерация
```

## Новый флоу (единый)

```
Любое место:
  [Список стилей из style_presets_v2] → клик на стиль → превью:
      [Стикер-пример]
      🎌 Аниме
      Описание стиля...
      [✅ ОК]
  → [✅ ОК] → применить стиль → продолжить flow
```

### Что видит пользователь

**Шаг 1 — Список стилей (данные из `style_presets_v2`):**
```
🎨 Выбери стиль:

[🎌 Аниме]    [🖍️ Мультфильм] [📷 Реалистичный]
[🖼️ Масло]    [💧 Акварель]    [✏️ Карандаш]
[🎯 Поп-арт]  [🌆 Киберпанк]   [📺 Винтаж]
[🖤 Нуар]     [🎬 Кинематограф] [👾 Пиксель-арт]
[🏺 Пластилин] [🧱 LEGO]
[⬅️ Назад]
```

**Шаг 2 — Клик на стиль → превью (стикер + описание):**
```
[Стикер-пример из getStyleStickerFileId]

🎌 Аниме

Классический аниме-стиль — большие выразительные глаза,
тонкие линии, нежные цвета и атмосфера студии Ghibli.

[✅ ОК]
```

Стикер-пример — из `stickers` таблицы (`is_example = true` или fallback на любой стикер этого стиля).
Если стикера нет — показать только текст с описанием.

**Шаг 3 — [✅ ОК] → возврат к предыдущему экрану с новым стилем.**

## Технические изменения

### 1. SQL: поле `description_ru` в `style_presets_v2`

```sql
ALTER TABLE style_presets_v2
  ADD COLUMN IF NOT EXISTS description_ru text;
```

Миграция: `sql/074_style_preview_card.sql` — уже создана.

### 2. `StylePresetV2` interface

```typescript
interface StylePresetV2 {
  // ... existing fields ...
  description_ru: string | null;  // NEW
}
```

Уже сделано в `feature/style-preview-card`.

### 3. Меню идей: `asst_idea_restyle` + `asst_idea_restyle_ok`

#### `asst_idea_restyle` — изменённая логика

**Было:** клик → сразу применить стиль → showStickerIdeaCard.

**Стало:** клик → отправить стикер-пример + текст с описанием + кнопка [✅ ОК].

```typescript
bot.action(/^asst_idea_restyle:([^:]+):(\d+)$/, async (ctx) => {
  safeAnswerCbQuery(ctx);
  // 1. Получить user, session, lang
  // 2. Получить preset по styleId (getStylePresetV2ById)
  // 3. Удалить сообщение со списком стилей (ctx.deleteMessage)
  // 4. Отправить стикер-пример (getStyleStickerFileId → ctx.replyWithSticker)
  //    — запомнить stickerMsgId
  // 5. Отправить текст:
  //    "{emoji} *{name_ru}*\n\n{description_ru || prompt_hint}"
  //    + кнопка [✅ ОК] → asst_idea_restyle_ok:{styleId}:{ideaIndex}:{stickerMsgId}
});
```

#### Новый `asst_idea_restyle_ok`

```typescript
bot.action(/^asst_idea_restyle_ok:([^:]+):(\d+):(\d+)$/, async (ctx) => {
  safeAnswerCbQuery(ctx);
  // 1. Получить user, session
  // 2. Получить preset по styleId
  // 3. Обновить sticker_ideas_state.styleId в session
  // 4. Удалить стикер-превью по stickerMsgId
  // 5. Удалить текущее сообщение (описание + кнопка ОК)
  // 6. Показать showStickerIdeaCard с новым стилем
});
```

### 4. Manual mode: `sendStyleKeyboardFlat`

**Было:** кнопки `style_v2:{id}` — клик сразу запускает генерацию.

**Стало:** кнопки `style_preview:{id}` — клик показывает превью.

```typescript
// Изменить callback в кнопках:
callback_data: `style_preview:${preset.id}`  // было: style_v2:${preset.id}

// Убрать кнопку "Пример" (style_example_v2) — превью теперь встроено
// Layout: 2 стиля в ряд
```

Уже сделано в `feature/style-preview-card`.

### 5. Manual mode: `style_preview` + `back_to_style_list`

#### `style_preview` — уже реализован

```typescript
bot.action(/^style_preview:(.+)$/, async (ctx) => {
  // 1. Проверить user, session, state === "wait_style"
  // 2. Получить preset (getStylePresetV2ById)
  // 3. Отправить стикер-пример (getStyleStickerFileId → ctx.replyWithSticker)
  // 4. Отправить текст: "{emoji} *{name_ru}*\n\n{description_ru}"
  //    + кнопки: [✅ Применить стиль] → style_v2:{id}
  //              [↩️ Назад] → back_to_style_list:{stickerMsgId}
});
```

#### `back_to_style_list` — уже реализован

```typescript
bot.action(/^back_to_style_list:(\d+)?$/, async (ctx) => {
  // 1. Удалить стикер-превью
  // 2. Удалить текущее сообщение
  // 3. sendStyleKeyboardFlat заново
});
```

### 6. Carousel: `sendStyleCarousel`

**Было:** кнопки `style_carousel_pick:{id}`.

**Стало:** кнопки `style_preview:{id}` — единый handler с manual mode.

Уже сделано в `feature/style-preview-card`.

### 7. Описания стилей (контент из `style_presets_v2.description_ru`)

| ID | Emoji | Название | Описание (RU) |
|----|-------|----------|---------------|
| anime | 🎌 | Аниме | Классический аниме-стиль — большие выразительные глаза, тонкие линии, нежные цвета и атмосфера студии Ghibli. |
| cartoon | 🖍️ | Мультфильм | Мультяшный стиль — яркие цвета, утрированные черты лица, дружелюбное выражение. |
| realistic | 📷 | Реалистичный | Фотореалистичная обработка — максимум деталей, профессиональное освещение, как с фотосессии. |
| oil_painting | 🖼️ | Масло | Стиль масляной живописи — богатые текстуры, мазки кисти, драматичное освещение, как классический портрет. |
| watercolor | 💧 | Акварель | Акварельный стиль — мягкие переходы цветов, воздушность, текстура бумаги. |
| pencil_sketch | ✏️ | Карандаш | Карандашный набросок — детальная штриховка, графитовый рисунок, реалистичные тени. |
| pop_art | 🎯 | Поп-арт | Поп-арт — яркие контрастные цвета, полутоновые точки, стиль Энди Уорхола. |
| cyberpunk | 🌆 | Киберпанк | Киберпанк — неоновые огни, футуристическая атмосфера, высокие технологии и тёмные улицы. |
| vintage | 📺 | Винтаж | Винтажная фотография — тёплые тона, зернистость плёнки, ретро-обработка в стиле 70-х. |
| noir | 🖤 | Нуар | Нуар — драматичный чёрно-белый стиль, высокий контраст, кинематографичные тени. |
| cinematic | 🎬 | Кинематограф | Кинематографический стиль — как кадр из фильма, анаморфотная оптика, цветокоррекция. |
| pixel_art | 👾 | Пиксель-арт | Пиксель-арт — ретро-графика в стиле 16-битных игр, ограниченная палитра. |
| clay | 🏺 | Пластилин | 3D-стиль из пластилина — объёмные фигурки, мягкое освещение, как в стоп-моушн анимации. |
| lego | 🧱 | LEGO | Стиль LEGO — пластиковые кубики, яркие цвета, игрушечная эстетика. |

## Сводка: что уже сделано / что осталось

| Компонент | Статус |
|-----------|--------|
| SQL миграция 074 | ✅ Сделано |
| `StylePresetV2.description_ru` | ✅ Сделано |
| `sendStyleKeyboardFlat` → `style_preview` | ✅ Сделано |
| `sendStyleCarousel` → `style_preview` | ✅ Сделано |
| Handler `style_preview` (manual mode) | ✅ Сделано |
| Handler `back_to_style_list` (manual mode) | ✅ Сделано |
| Изменить `asst_idea_restyle` → показать превью | TODO |
| Новый handler `asst_idea_restyle_ok` | TODO |

## Оценка оставшейся работы

| Шаг | Время |
|-----|-------|
| Изменить `asst_idea_restyle` → превью (стикер + описание + ОК) | 15 мин |
| Новый handler `asst_idea_restyle_ok` | 15 мин |
| Тест + правки | 15 мин |
| **Итого** | **~45 мин** |
