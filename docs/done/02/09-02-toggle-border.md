# Toggle Border — Кнопка обводки после генерации

## Цель

Вынести добавление белой обводки (border) из ассистент-флоу в post-processing кнопку после генерации стикера. Обводка добавляется через `sharp` (пост-обработка), не через Gemini. **Бесплатно, без списания кредитов.**

## Обзор

| Аспект | Было | Станет |
|--------|------|--------|
| Где спрашивается border | Ассистент, шаг 6: "Добавить белую обводку?" | Кнопка `🔲 Обводка` после генерации |
| Как добавляется | Gemini prompt: "Bold white outline/border..." | sharp post-processing: dilate alpha → white stroke |
| Стоимость | 1 кредит (полная re-генерация) | 0 кредитов (image processing) |
| Цвет | Белый (из промпта) | Белый (hardcoded, v1) |

---

## Часть 1: Убрать border из ассистент-флоу

**Файл: `src/lib/ai-chat.ts`**

1. Убрать `border` из `ASSISTANT_TOOLS[0].parameters.properties` (строка 67)
2. Убрать шаг 6 из system prompt (строки 189-191) — "After collecting style, emotion, pose — ask if user wants a white border..."
3. Убрать `– **Border:** yes/no` из Mirror Message Format (строка 209)
4. Шаг 7 теперь: "When all 3 main params collected — show mirror, then STOP"

**Файл: `src/index.ts`**

5. `buildMirrorMessage()`: убрать `borderText` и строку `– **Обводка / Border**`
6. `buildAssistantPrompt()`: убрать параметр `border` из сигнатуры, убрать `borderLine`, всегда генерировать **без обводки** (промпт: "No outline/border around the character")

**Файл: `src/lib/assistant-db.ts`**

7. `handleToolCall()`: оставить `if (args.border !== undefined) updates.border = Boolean(args.border)` для обратной совместимости, но AI больше не будет передавать border
8. Поле `border` в `AssistantSessionRow` — оставить, не ломать миграции

---

## Часть 2: Кнопка `toggle_border` после генерации

**Файл: `src/lib/texts.ts`**

9. Добавить:
   - ru: `"btn.toggle_border": "🔲 Обводка"`
   - en: `"btn.toggle_border": "🔲 Border"`

**Файл: `src/worker.ts`**

10. Добавить получение текста кнопки:
    ```typescript
    const toggleBorderText = await getText(lang, "btn.toggle_border");
    ```

11. Добавить кнопку в `replyMarkup.inline_keyboard`:
    ```
    [Добавить в пак]
    [Ассистент] [Изменить эмоцию]
    [Изменить движение] [Обводка]
    [Добавить текст]
    ```

---

## Часть 3: Image processing — функция `addWhiteBorder`

**Файл: `src/worker.ts`** (или отдельный `src/lib/image-utils.ts`)

12. Новая функция `addWhiteBorder(inputBuffer: Buffer, borderWidth?: number): Promise<Buffer>`

**Алгоритм:**
1. Загрузить PNG/WebP с alpha-каналом через `sharp`
2. Извлечь alpha channel
3. Dilate alpha (расширить на `borderWidth` px) — морфологическая дилатация по кругу
4. Создать белый слой по dilated alpha (белый пиксель где dilated > 0)
5. Composite: белый контур (снизу) + оригинал (сверху)
6. Trim + resize 512×512 + webp

**Вспомогательная функция `dilateAlpha(alpha, width, height, radius): Buffer`:**
- Для каждого пикселя: если в радиусе `radius` есть непрозрачный пиксель (alpha > 0) — выставить max alpha
- Форма: круг (`dx*dx + dy*dy <= radius*radius`)

---

## Часть 4: Handler `toggle_border` в index.ts

**Файл: `src/index.ts`**

13. Новый handler `bot.action(/^toggle_border:(.+)$/, ...)`:

    1. Получить стикер из БД по `stickerId` (`telegram_file_id`, `source_photo_file_id`, `user_id`)
    2. Проверить принадлежность стикера пользователю
    3. Скачать текущий стикер (webp) через Telegram file API (`getFilePath` → `downloadFile`)
    4. Вызвать `addWhiteBorder(stickerBuffer)`
    5. Отправить новый стикер пользователю с теми же кнопками (`sendSticker`)
    6. Обновить `telegram_file_id` в таблице `stickers`
    7. Upload bordered версии в Storage (background, non-blocking)

---

## Часть 5: Параметры обводки

| Параметр | Значение | Комментарий |
|----------|----------|-------------|
| Цвет | `#FFFFFF` (белый) | Hardcoded в v1, потом можно расширить |
| Толщина | 8px | На стикере 512×512 это хорошо видно |
| Форма | Круглая (по радиусу) | `dx*dx + dy*dy <= radius*radius` |
| Стоимость | 0 кредитов | Пост-обработка, без Gemini |

---

## Не делаем (scope out)

- Выбор цвета обводки (будет в v2)
- Выбор толщины (будет в v2)
- Снятие обводки (toggle off) — можно добавить потом, если хранить оригинал
- SQL миграция — не нужна, данные стикера не меняются (обновляем только `telegram_file_id`)

---

## Риски

| Риск | Митигация |
|------|-----------|
| Dilate на 512×512 может быть медленным | O(n × radius²), для radius=8 и 512×512 ~ 16M ops, ~100ms. Приемлемо |
| Telegram file_id перезаписывается | Оригинал теряется. Можно хранить `original_telegram_file_id` в stickers, но для v1 достаточно |
| WebP alpha может быть неточным | Sharp корректно обрабатывает WebP alpha. Тестировать на реальных стикерах |
