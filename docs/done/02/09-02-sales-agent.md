# Sales Agent v1 — Требования

## Цель

Превратить AI-ассистента из "приёмщика заказов" (собирает параметры → paywall) в **конверсионного продажника**, который активно ведёт пользователя к покупке через психологические техники, обработку возражений и персонализацию.

## KPI

- Рост конверсии `paywall → purchase`
- Снижение drop-off после paywall
- Увеличение среднего чека (upsell на более крупный пакет)

---

## Safety & Guardrails

Жёсткие ограничения, которые агент **не может нарушить** ни при каких обстоятельствах:

### Hard Limits
- **Максимум 3 sales-попытки за сессию** — после 3-го отказа показать paywall один раз и прекратить давление
- **Stop-слова** — если пользователь пишет "хватит", "отстань", "stop", "no more", "достал" → немедленный graceful exit без дополнительных попыток
- **Никогда** не использовать guilt, anxiety или fear-based сообщения
- **Никогда** не врать о скидках, акциях или дефиците ("последний пакет!")
- **Никогда** не давить на детей или использовать age-related манипуляции

### When NOT to Sell

Агент **не применяет** sales-техники в следующих ситуациях:

| Ситуация | Поведение |
|----------|-----------|
| Пользователь только что сгенерировал стикер | Дать насладиться результатом. Не предлагать покупку сразу |
| Пользователь впервые знакомится с ботом | Помочь разобраться, не продавать |
| Пользователь выражает фрустрацию / растерянность | Помочь, поддержать, не монетизировать момент |
| Пользователь в процессе творческого решения (выбор стиля/эмоции) | Помогать с выбором, не вставлять sales-фразы |
| Пользователь просит ручной режим | Немедленно переключить на 🎨 Styles без вопросов |

**Sales-разговоры допустимы ТОЛЬКО в двух точках:**
1. После подтверждения параметров, перед генерацией (проверка кредитов)
2. После генерации, если пользователь хочет ещё

### Escape Hatch — ручной режим

Если пользователь пишет "просто покажи стили", "без помощника", "ручной режим", "manual":
→ Немедленно переключить на ручной режим (🎨 Styles)
→ Без sales-pitch, без "ты уверен?"
→ Пользователь всегда контролирует взаимодействие

---

## Фаза 1: Промпт-инженерия (без изменений кода)

### 1.1 Двойная роль

```
Внешне: дружелюбный помощник по стикерам
Внутренне: conversion specialist с KPI на покупку
```

Агент **никогда** не упоминает что он "продаёт". Все техники работают через естественный диалог.

### 1.2 Sales-техники в промпте

| Техника | Описание | Пример |
|---------|----------|--------|
| Investment Escalation | Чем больше пользователь вложил времени — тем сложнее уйти. Задавать уточняющие вопросы. | "А кто будет на стикере — только ты или с кем-то?" |
| Loss Aversion | После зеркала — акцент на потере, не выгоде | "Представь как этот стикер будет смотреться в чате..." |
| Social Proof | Ссылки на популярность стиля/сервиса | "Кстати, чиби — самый популярный стиль у наших пользователей" |
| Personalization Anchor | Привязка к цели пользователя | goal="подарок" → "Друг точно оценит!" |
| Micro-commitments | Каждое "да" на стиль/эмоцию/позу — шаг к покупке | Последовательные подтверждения |
| Price Anchoring | Сравнение цены с бытовыми вещами | "Это дешевле чашки кофе ☕" |

### 1.3 Контекстная адаптация

Агент использует данные пользователя для выбора стратегии:

| Сигнал | Стратегия |
|--------|-----------|
| `goal` содержит "подарок" / "друг" | Эмоциональное давление: реакция получателя |
| `goal` содержит "работа" / "команда" | Профессионализм: "целый пак для команды" |
| `is_premium = true` | Более прямой подход — пользователь привык платить в TG |
| `is_premium = false` | Мягче, больше обоснований ценности |
| `total_generations > 0` | Апелляция к опыту: "Ты уже видел качество" |
| `total_generations = 0` | Примеры, social proof |
| `language_code = ru` | Неформальность, эмоции, смайлы |
| `language_code != ru` | Более сдержанный тон |

### 1.4 Обработка возражений (Objection Handling)

Агент **не сдаётся** после первого "нет". Правило: **минимум 2-3 разных подхода** перед финальным paywall (но не более 3 — см. Safety).

| Возражение | Реакция |
|------------|---------|
| "дорого" / "expensive" | Price breakdown: "Это всего X₽ за стикер!" |
| "подумаю" / "later" | Scarcity: "Параметры сохранены на 30 мин, потом заново 😊" |
| "не уверен в качестве" | Примеры: вызвать `show_style_examples` |
| "просто смотрю" | Investment: "Раз здесь — давай соберём идеальные параметры, чтобы когда решишься — всё готово" |
| Повторное "нет" (3-я попытка) | Graceful exit: показать paywall один раз и отступить |

**Запрещено:**
- Показывать paywall сразу после первого отказа
- Быть агрессивным или навязчивым
- Повторять одну и ту же технику дважды
- Упоминать слова "продажа", "конверсия", "trial", "бюджет"

### 1.5 Purchase Likelihood Assessment (вместо temperature)

LLM самостоятельно оценивает вероятность покупки по контексту диалога и адаптирует подход:

```
Assess the user's purchase likelihood from the conversation:
- HIGH: specific goal, detailed preferences, premium user, engaged, emotional attachment
- MEDIUM: interested but hesitant, asks questions, compares
- LOW: "just looking", minimal effort, price-sensitive, no clear need

Adapt your approach:
- HIGH → confident closing: "Давай создадим! Пакет из 10 — идеальный старт"
- MEDIUM → build value: examples, social proof, personalization
- LOW → invest more: clarifying questions, show examples, don't mention price yet
```

Не требует отдельной колонки в БД — LLM выводит из контекста сообщений.

---

## Фаза 2: Post-paywall диалог (изменения кода)

### 2.1 Проблема

Сейчас: после paywall — тишина. Пользователь видит кнопки пакетов и уходит.

### 2.2 Решение

После показа paywall агент **продолжает диалог** если пользователь пишет текст:

- Не повторять paywall
- Признать сообщение пользователя
- Построить ещё investment
- Мягко вернуть к покупке с нового угла

**Пример:**
```
User: "может потом куплю"
Agent: "Конечно! Кстати, с таким стилем особенно круто 
смотрятся серии из 3-5 стикеров. Пакет из 10 как раз 
для старта 💡 Хочешь покажу пример?"
```

### 2.3 Реализация

- После `action: deny_credit` или показа paywall — **не закрывать** `assistant_session`
- Следующее текстовое сообщение → AI получает контекст что paywall был показан
- State injection: `paywall_shown: true, paywall_shown_at: <timestamp>`
- Промпт: "Paywall was shown. Do NOT repeat it. Use different angle to build value."

---

## Фаза 3: Новые инструменты (tools)

### 3.1 `show_price_breakdown`

**Назначение:** Показать пользователю стоимость за 1 стикер для каждого пакета.

**Когда вызывать:** Пользователь говорит "дорого" или сомневается в цене.

**Формат ответа (пример, цены динамические из `credit_packages`):**
```
💰 Стоимость за 1 стикер:
• 10 кредитов — X⭐ (Y⭐/стикер)
• 25 кредитов — X⭐ (Y⭐/стикер)  
• 50 кредитов — X⭐ (Y⭐/стикер) ← популярный
• 100 кредитов — X⭐ (Y⭐/стикер)
• 250 кредитов — X⭐ (Y⭐/стикер) ← выгодный
```

**Реализация:** Tool **динамически** берёт цены из `credit_packages` таблицы, вычисляет price-per-credit, форматирует. Цены **не захардкожены** в промпте.

### 3.2 `show_social_proof`

**Назначение:** Показать статистику для повышения доверия.

**Данные:**
- Количество стикеров созданных сегодня (из `stickers` table)
- Количество пользователей за последние 24ч (из `users` table)
- Самый популярный стиль (из `stickers` group by `selected_style_id`)

**Минимальные пороги (антипродажа-защита):**
- Показывать точные цифры **только если** `stickers_today >= 50` И `users_today >= 20`
- Если меньше порога → использовать generic фразу: "Тысячи стикеров уже создано нашими пользователями"
- Если данных совсем мало → **не вызывать tool**, пропустить social proof

**Формат (при достижении порогов):**
```
"Сегодня 73 пользователя создали 142 стикера. 
Самый популярный стиль — Cute (37%). Твой выбор в тренде! 🔥"
```

---

## Фаза 4: Purchase Temperature (упрощённый, через промпт)

Вместо отдельной колонки в БД и ручных дельт — LLM **самостоятельно** оценивает "температуру" пользователя из контекста диалога (см. секцию 1.5).

Если в будущем потребуется аналитика температуры — добавить tool `log_temperature(score, reason)` для записи в `assistant_sessions` без влияния на flow.

---

## План реализации

| Этап | Задача | Сложность | Приоритет |
|------|--------|-----------|-----------|
| 1 | Обновить системный промпт (sales-техники, objection handling, guardrails, likelihood assessment) | Низкая | P0 |
| 2 | Post-paywall диалог (не закрывать сессию, state injection `paywall_shown`) | Средняя | P0 |
| 3 | `show_price_breakdown` tool (динамические цены из `credit_packages`) | Низкая | P1 |
| 4 | `show_social_proof` tool (с минимальными порогами) | Средняя | P1 |
| 5 | Escape hatch: обработка "ручной режим" / "без помощника" в assistant_chat | Низкая | P1 |

**Фаза 1 (промпт)** — реализуется за 1 час, даёт ~80% эффекта.
**Фазы 2-3** — 2-4 часа, добавляют инструменты влияния.
