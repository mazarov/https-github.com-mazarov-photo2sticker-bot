# Праздничная кнопка на карусели паков (8 марта)

**Дата:** 27.02.2026  
**Статус:** Требования для разработки  
**Ветка:** `feature/27-02-pack-carousel-holiday-8march` (по правилам git-workflow)  
**Связано:** `src/index.ts` (handlePackMenuEntry, карусель), `holiday_themes`, `pack_content_sets_test`, `docs/14-02-ideas-improvements.md`

## Цель

На **первом шаге** флоу генерации паков (карусель наборов, состояние `wait_pack_carousel`) добавить кнопку переключения праздничного режима «С 8 марта», по аналогии с кнопкой праздника в флоу идей ассистента. При включении показывать только наборы с заданным `pack_template_id` (8 марта) из `pack_content_sets_test`.

Отдельная запись в `holiday_themes` для паков **не создаётся** — используется существующая запись `id = 'march_8'` («С 8 марта»).

---

## 1. Данные

### 1.1. Таблица `holiday_themes`

- Используется **существующая** запись `id = 'march_8'` (emoji 🌷, name_ru «С 8 марта», name_en «Women's Day» и т.д.).
- Для отображения кнопки на карусели паков проверять запись с `id = 'march_8'` и `is_active = true`. Если не активна — кнопку не показывать.
- Связь «праздник → шаблон пака»:
  - **Вариант A:** в `pack_content_sets_test` у праздничных наборов задать `pack_template_id = 'march_8'` (то же значение, что id в holiday_themes). Тогда маппинг не нужен.
  - **Вариант B:** если шаблон в БД уже называется иначе (например `8march`), в коде задать маппинг: `march_8` → `'8march'` (или добавить в `holiday_themes` колонку `pack_template_id` и хранить там значение для паков).

### 1.2. Таблица `pack_content_sets_test`

- В ней должны быть наборы с выбранным `pack_template_id` (`march_8` или `8march`) и `is_active = true`.
- На test окружении используется `config.packContentSetsTable` (= `pack_content_sets_test` при `APP_ENV === "test"`).

---

## 2. Поведение в боте

### 2.1. Место в UI

- **Экран:** карусель наборов пака (состояние сессии `wait_pack_carousel`).
- **Текущая клавиатура:** строка «◀️ / N/M / ▶️», строка «Попробовать», при необходимости admin-ряд.
- **Добавить:** строку с одной кнопкой переключения праздника, по аналогии с ассистентом: «🌷 С 8 марта: off» / «🌷 С 8 марта: on».

### 2.2. Когда показывать кнопку

- Кнопка показывается только если в `holiday_themes` есть запись с `id = 'march_8'` и `is_active = true`. Иначе строку с кнопкой не выводить.

### 2.3. Состояние «праздник включён»

- Хранить в сессии пак-флоу. Рекомендуется колонка в `sessions`: `pack_holiday_id text` (nullable). При включении — `'march_8'`, при выключении — `null`.
- Альтернатива: хранить в существующем JSON-поле сессии (если есть расширяемая структура по пак-флоу).

### 2.4. Логика отображения наборов

- При отображении карусели:
  - если `pack_holiday_id = 'march_8'`: показывать только наборы из `pack_content_sets_test` с выбранным `pack_template_id` (см. п. 1.1: `march_8` или значение из маппинга);
  - если `pack_holiday_id` пустой: текущее поведение — «дефолтный» шаблон (например, первый из `getActivePackContentSets()` или сохранённый при создании сессии).
- Существующую фильтрацию по subject_mode (если включена) применять к отфильтрованному по шаблону списку как и сейчас.

### 2.5. Callback и перерисовка

- **Включение:** callback вида `pack_holiday:march_8` (или `pack_holiday_on:march_8`).
- **Выключение:** callback `pack_holiday_off` (или `pack_holiday_off:march_8`).
- Обработчик: обновить сессию (`pack_holiday_id`), пересобрать `visibleSets` с учётом праздника, перерисовать карусель (тот же экран, обновлённый список наборов).

### 2.6. Дальнейший флоу

- При нажатии «Попробовать» сохраняется выбранный набор (`pack_content_set_id`) и текущий `pack_template_id` из выбранного сета. Дальнейшие шаги (фото, стиль, превью, оплата) без изменений.

---

## 3. Миграции

- **Сессии:** при необходимости добавить колонку `pack_holiday_id text` в таблицу `sessions` (новая миграция в `sql/`).
- **holiday_themes:** изменений не требуется (используем `march_8`).
- **pack_content_sets_test:** наличие наборов с нужным `pack_template_id` — отдельная задача/миграция при необходимости.

---

## 4. Привязка к коду

| Что | Файл | Место |
|-----|------|--------|
| Вход в пак, создание сессии, первый показ карусели | `src/index.ts` | `handlePackMenuEntry` (~3703), вставка сессии с `pack_template_id`, построение `visibleSets` |
| Загрузка наборов | `src/index.ts` | `getActivePackContentSets()` (~134), таблица `config.packContentSetsTable` |
| Фильтр по шаблону | `src/index.ts` | `getPackContentSetsForTemplate(allContentSets, session.pack_template_id)` (~206) |
| Рендер карусели | `src/index.ts` | `renderPackCarouselForSession` (~4549), обработчики `pack_carousel_prev` / `pack_carousel_next`, `pack_try:*` |
| Праздник в ассистенте (аналог) | `src/index.ts` | `getActiveHoliday()` (~436), кнопка в `showStickerIdeaCard` (~10550), callback `asst_idea_holiday`, `asst_idea_holiday_off` |
| Таблица праздников | `sql/072_ideas_improvements.sql` | создание `holiday_themes`, запись `march_8` |

---

## 5. Чеклист реализации

- [ ] Миграция: добавить в `sessions` колонку `pack_holiday_id text` (если выбран этот способ хранения).
- [ ] При первом показе карусели и при рендере: получать активный праздник для паков (запись `holiday_themes` с `id = 'march_8'` и `is_active = true`); при наличии — добавить в клавиатуру строку с кнопкой «🌷 С 8 марта: off/on».
- [ ] Определить эффективный `pack_template_id` для карусели: при `pack_holiday_id = 'march_8'` использовать `'march_8'` (или значение из маппинга); иначе — текущий дефолтный шаблон сессии.
- [ ] Добавить обработчики callback `pack_holiday:march_8` и `pack_holiday_off`: обновление `pack_holiday_id` в сессии, пересборка `visibleSets`, перерисовка карусели.
- [ ] Убедиться, что при создании сессии при входе в пак поле `pack_holiday_id` инициализируется (null); при возврате в карусель состояние переключателя восстанавливается из сессии.
- [ ] Обновить `docs/architecture/01-api-bot.md`: описание кнопки праздника на карусели паков (при наличии).

---

## 6. Критерии готовности

- В карусели паков при активной записи `holiday_themes.id = 'march_8'` отображается кнопка «🌷 С 8 марта: off/on».
- Включение переключает карусель на наборы только с выбранным `pack_template_id` (march_8 или маппинг); выключение возвращает к обычному списку.
- Состояние переключателя сохраняется в сессии и не теряется при листании карусели и возврате на экран.
