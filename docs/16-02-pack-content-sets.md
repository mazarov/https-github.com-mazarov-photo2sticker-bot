# Требования: наборы подписей и сцен для пака (таблица готовых наборов)

**Дата:** 16.02.2026  
**Проект:** photo2sticker-bot  
**Связано:** [15-02-pack-flow-requirements.md](15-02-pack-flow-requirements.md)

**История решений:** Изначально рассматривали генерацию сцен и подписей через LLM в момент нажатия «Посмотреть превью». Отказались в пользу таблицы готовых наборов: стабильный контент, возможность модерации, предсказуемый результат для пользователя. LLM можно использовать позже для создания новых вариантов наборов (с последующей модерацией и добавлением в таблицу).

---

## 1. Цель

Дать пользователю **выбор набора подписей и сцен** (или подбирать набор автоматически по стилю), чтобы пак был разнообразным и предсказуемым. Контент хранится в таблице **готовых наборов** (`pack_content_sets`), привязанной к шаблону пака. Пользователь либо выбирает набор кнопками после выбора стиля, либо получает набор по автоподбору.

---

## 2. Таблица готовых наборов

### 2.1. Структура `pack_content_sets`

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | text | PK, например `couple_v1_romance`, `couple_v1_humor` |
| `pack_template_id` | text | FK на `pack_templates.id` (например `couple_v1`) |
| `name_ru` | text | Название набора для пользователя: «Романтика», «С юмором» |
| `name_en` | text | Название на английском |
| `labels` | jsonb | Массив из N строк — подписи на русском (порядок = порядок стикеров) |
| `labels_en` | jsonb | Массив из N строк — подписи на английском |
| `scene_descriptions` | jsonb | Массив из N строк (EN) — визуальные описания сцен для Gemini |
| `sort_order` | int | Порядок показа в выборе (1, 2, 3…) |
| `is_active` | boolean | Активен ли набор |
| `mood` | text | Опционально: для автоподбора по стилю — `romance`, `everyday`, `humor`, `support`, `sarcasm`, `reactions`, `holiday` |
| `created_at` | timestamptz | — |

Ограничения:

- Длина массивов `labels`, `labels_en`, `scene_descriptions` должна совпадать с `pack_templates.sticker_count` для данного шаблона (4 или 16).
- Подпись: короткая фраза (1–3 слова, до ~40 символов), без переносов строк.
- `scene_descriptions[i]` — одно предложение на английском, визуальное описание сцены под подпись `labels[i]`.

### 2.2. Связь с шаблоном

- У одного `pack_template_id` может быть несколько наборов (например, для `couple_v1`: Романтика, Быт, С юмором и т.д.).
- В `pack_templates` по-прежнему хранятся дефолтные `labels`, `labels_en`, `scene_descriptions` — они используются как **fallback**, если у пользователя не выбран набор или набор не найден.

---

## 3. Наборы для РФ (темы для шаблона «Пара»)

Рекомендуемые названия наборов и характер подписей/сцен (контент заполняется отдельно — миграциями или админкой):

| name_ru | name_en | mood | Описание |
|---------|--------|------|----------|
| Романтика | Romance | romance | Тёплые фразы: люблю, навсегда, подарок, вместе, чмок, красотка, мой герой. Сцены: объятия, поцелуй в щёку, руки вместе, букет. |
| Быт и уют | Everyday | everyday | Домашние ситуации: спим?, где еда?, вырубайся, устал, диван, мимими. Сцены: зевает, обнялись на диване, смотрит в холодильник. |
| С юмором | With humor | humor | Подколы: опять ты, ну привет, ну ты даёшь, выручай, кранты. Сцены: закатывает глаза, дурашливые позы, «рука в лицо». |
| Поддержка | Support | support | Мы команда: мой герой, вместе справимся, горжусь, ты сможешь, рядом. Сцены: объятие сзади, рука на плече, два кулака вместе. |
| Ласка и комплименты | Sweet | romance | Красотка, милый, таю, обожаю, котик, солнышко. Сцены: нежные объятия, поцелуй в лоб, смущённая улыбка. |
| Сарказм | Sass | sarcasm | Ага конечно, ну да, всё ясно, очень верю. Сцены: скрещённые руки, поднятая бровь, ироничный взгляд. |
| На каждый день | Daily reactions | reactions | Доброе утро, скучаю, устал, голоден, на работе, спокойной ночи. Сцены: потягивается, зевает, телефон в руке. |
| Праздник | Holiday | holiday | С днём рождения, с 14 февраля, с годовщиной, поздравляю, за нас. Сцены: с подарком, с цветами, бокалы, конфетти. |

Количество активных наборов на старте можно ограничить (например, 3–4), остальные добавлять по мере готовности контента.

---

## 4. Пользовательский флоу (изменённый)

Меняем порядок шагов перед превью: сначала приглашение посмотреть пак, затем карусель шаблона с описанием наборов фраз/эмоций.

### 4.1. Шаг 1 — приглашение

Показываем один экран с текстом (локализация RU/EN):

- **EN:** «Want to see how your sticker pack will look?»
- **RU:** например «Хочешь посмотреть, как будет выглядеть твой стикерпак?»

На этом шаге пользователь понимает, что дальше ему покажут варианты пака. Кнопка CTA ведёт на шаг 2 (карусель шаблона). Опционально: на шаг 1 можно показать одну кнопку «Да, показать» / «Попробовать» и по нажатию перейти к шагу 2.

### 4.2. Шаг 2 — карусель шаблона с описанием наборов

Показываем **карусель** (несколько карточек, переключаемых кнопками ◀️ ▶️ или свайпом):

- **Заголовок / контекст:** название шаблона (например **Couple**) и общее описание (например «Unique Telegram stickers for your couple 💖»). Может быть общим для всей карусели или повторяться на каждой карточке.
- **Содержимое карусели:** каждая карточка = один набор из `pack_content_sets` для данного шаблона. На карточке — **короткое описание**, какие фразы/эмоции входят в этот набор (например: «Тёплые фразы: люблю, навсегда, подарок, чмок…» или «Домашние ситуации: спим?, где еда?, вырубайся…»).

Цель: пользователь листает карточки и понимает, какой набор фраз/эмоций ему ближе, затем выбирает набор (или подтверждает дефолтный) и переходит к «Пришли фото» → выбор стиля → «Посмотреть превью».

**Данные для карусели:**

- Для каждой записи в `pack_content_sets` нужен **короткий текст для карточки** (1–2 предложения): какие фразы/эмоции входят в набор. Варианты хранения:
  - отдельные поля в `pack_content_sets`: например `carousel_description_ru`, `carousel_description_en`, либо переиспользовать одно поле `description_ru` / `description_en`, если оно уже есть или планируется для карточки.
- Шаблон: `pack_templates.name_ru` / `name_en`, `pack_templates.description_ru` / `description_en` — для заголовка и подзаголовка карусели («Couple», «Unique Telegram stickers for your couple 💖»).

**Кнопки на карусели:**

- Переключение карточек: `[◀️]` / `[▶️]` (или одна кнопка «Попробовать» на каждой карточке с привязкой к выбранному набору).
- По выбору набора: сохраняем `pack_content_set_id` в сессию и переходим к следующему шагу (например «Пришли одно фото» или сразу к выбору стиля, в зависимости от общего флоу из 15-02).

### 4.3. Дальнейший флоу

После карусели — «Пришли одно фото» → выбор стиля (style_presets_v2) → «Посмотреть превью — 1 💎» → генерация превью и сборка пака. Подписи и сцены берутся из выбранного в карусели набора (`pack_content_set_id`); если не выбрано — fallback на данные шаблона (см. п. 5).

---

## 5. Встраивание в флоу (выбор набора и превью)

### 5.1. Место в сценарии

Флоу с учётом шагов 1–2:

```
[Сделать пак] → Шаг 1: «Want to see how your sticker pack will look?» → 
Шаг 2: Карусель (Couple + описание наборов фраз/эмоций) → выбор набора →
«Пришли одно фото» → Выбор стиля → [👀 Посмотреть превью — 1 💎] → …
```

- В карусели пользователь выбирает набор (или оставляем дефолтный); сохраняем `pack_content_set_id` в сессию.
- После выбора стиля и нажатия «Посмотреть превью» используются подписи и сцены из выбранного набора; если набора нет — из полей шаблона `pack_templates`.

### 5.2. Вариант A: явный выбор набора (альтернатива карусели)

- После выбора стиля (и при наличии фото) можно дополнительно показывать: «Какой набор подписей?» и inline-кнопки по наборам для данного шаблона.
- Callback: `pack_set:CONTENT_SET_ID`. По нажатию сохраняем в сессию `pack_content_set_id`. Затем пользователь нажимает «Посмотреть превью — 1 💎».

### 5.3. Вариант B: автоподбор по стилю (без выбора набора)

- Если пользователь не выбрал набор в карусели (или выбран дефолт), можно подбирать набор по правилу: маппинг `selected_style_id` → `mood` или предпочтительный `pack_content_set_id` для шаблона. Для сессии выставляем `pack_content_set_id` выбранного набора.
- При нажатии «Посмотреть превью» используются подписи и сцены из этого набора.

### 5.4. Гибрид

- В карусели пользователь выбирает набор или листает и нажимает «Попробовать» на понравившейся карточке — сохраняем `pack_content_set_id`. Опционально: после выбора стиля кнопка «Другой набор подписей» снова открывает карусель или список наборов.

---

## 6. Сессия и воркер

### 6.1. Сессия

- Добавить в `sessions` поле **`pack_content_set_id`** (text, nullable, FK на `pack_content_sets.id`).
- При выборе набора пользователем или при автоподборе записываем сюда id набора. Если null — воркер использует данные из `pack_templates`.

### 6.2. Воркер (превью и сборка пака)

- При генерации превью и при сборке пака:
  1. Если в сессии указан `pack_content_set_id`, загрузить запись из `pack_content_sets` по этому id.
  2. Если запись найдена, активна и длины массивов совпадают с `template.sticker_count` — использовать её `labels`, `labels_en`, `scene_descriptions`.
  3. Иначе — использовать `pack_templates.labels`, `pack_templates.labels_en`, `pack_templates.scene_descriptions` (текущее поведение).

Логика нарезки, rembg, оверлея подписей и сборки стикерпака не меняется; меняется только **источник** массивов подписей и сцен (набор из таблицы или шаблон).

---

## 7. Fallback и граничные случаи

- **Нет набора в сессии** (`pack_content_set_id` = null): использовать данные из `pack_templates` (как сейчас).
- **Набор удалён или неактивен:** при загрузке по `pack_content_set_id` запись не найдена или `is_active = false` — fallback на шаблон.
- **Несовпадение длины массивов** (например, в наборе 16 сцен, а в шаблоне sticker_count = 4): не использовать такой набор, fallback на шаблон.
- **У шаблона нет ни одного активного набора:** не показывать выбор наборов; в сессии `pack_content_set_id` остаётся null, везде используются данные шаблона.

---

## 8. Миграции и контент

- **Миграция 1:** создать таблицу `pack_content_sets` (структура из п. 2.1), при необходимости добавить в `sessions` колонку `pack_content_set_id` и FK.
- **Миграция 2 и далее:** заполнить наборы для `couple_v1` (и при необходимости для других шаблонов). Для каждого набора — 16 (или 4) элементов в `labels`, `labels_en`, `scene_descriptions`. Контент можно подготавливать вручную или с помощью LLM с последующей вычиткой и вставкой в БД.

---

## 9. Критерии приёмки (кратко)

1. Таблица `pack_content_sets` создана и связана с `pack_templates`; в сессиях есть возможность хранить `pack_content_set_id`. В наборах есть поля для карусели: `carousel_description_ru`, `carousel_description_en` (или переиспользуются существующие описания).
2. Для шаблона «Пара» (couple_v1) есть хотя бы один активный набор с полными labels / labels_en / scene_descriptions и коротким описанием для карусели.
3. **Флоу:** сначала показывается шаг 1 («Want to see how your sticker pack will look?»), затем шаг 2 — карусель шаблона (например «Couple» + «Unique Telegram stickers for your couple 💖»), где каждая карточка — набор контента с коротким описанием фраз/эмоций; по выбору карточки сохраняется `pack_content_set_id`. Дальше — фото → стиль → превью.
4. При нажатии «Посмотреть превью» и при сборке пака воркер берёт подписи и сцены из выбранного набора; при отсутствии или невалидности набора — из полей шаблона.
5. Пользователь получает стабильный, предсказуемый набор подписей и сцен в зависимости от выбранной темы набора в карусели (или автоподбора).
