# Guided Onboarding Flow

## Цель
Показать пользователю полный цикл создания стикера: базовый стикер → эмоция → понимание ценности → покупка.

## Сценарий (User Journey)

### Шаг 1: Первый стикер (за кредит)
```
Пользователь: /start
Бот: "Привет! Отправь фото — я сделаю из него стикер 🎨"
     [Пользователю начислено 2 бесплатных кредита]

Пользователь: [отправляет фото]
Бот: "Выбери стиль:" [кнопки стилей]

Пользователь: [выбирает "Аниме"]
Бот: "⏳ Создаю стикер..."
Бот: [отправляет стикер БЕЗ кнопок действий]
     "🎉 Вот твой первый стикер!
      А теперь давай оживим его — добавь эмоцию:"
     [😂 Смех] [😎 Крутой] [😢 Грустный] [😡 Злой]
     [Пропустить →]
```

### Шаг 2: Эмоция (за кредит)
```
Пользователь: [выбирает "😂 Смех"]
Бот: "⏳ Добавляю эмоцию..."
Бот: [отправляет стикер с эмоцией + обычные кнопки]
     "🔥 Отлично! Теперь ты умеешь создавать живые стикеры.
      
      Ещё можно:
      🏃 Добавить движение
      💬 Написать текст на стикере
      
      Хочешь создать ещё?"
     [🛒 Купить кредиты] [📷 Новое фото]
```

### Шаг 3: Покупка или новое фото
```
Если "Купить кредиты":
  → показать тарифы

Если "Новое фото":
  Бот: "Отправь фото — сделаем новый стикер!"
  → нужны кредиты (2 бесплатных уже потрачены)
```

## Требования

### 1. База данных

```sql
ALTER TABLE users ADD COLUMN IF NOT EXISTS onboarding_step int DEFAULT 0;

-- 0 = не начат (новый пользователь)
-- 1 = первый стикер создан, ждём эмоцию
-- 2 = онбординг завершён
-- 99 = онбординг пропущен
```

### 2. Начисление кредитов

**При регистрации:**
- Создать транзакцию на **2 кредита** (триггер начислит)
- Это единственное начисление — больше бесплатных не будет

**Списание:**
- Обычная логика `deduct_credits()` для ВСЕХ генераций
- Никаких специальных проверок `isOnboardingFree`

### 3. onboarding_step — ТОЛЬКО для UI

`onboarding_step` управляет **только отображением**, не бесплатностью:

| onboarding_step | UI поведение |
|-----------------|--------------|
| 0 | После стикера: БЕЗ кнопок, показать онбординг-эмоции |
| 1 | После эмоции: показать финальное сообщение |
| 2, 99 | Обычный режим: все кнопки, рейтинг |

### 4. Логика в worker.ts

**При `onboarding_step === 0` и `generationType === "style"`:**
```typescript
// Стикер БЕЗ кнопок (без reply_markup)
await sendSticker(telegramId, stickerBuffer, undefined);

// НЕ показывать рейтинг

// Показать онбординг-сообщение с эмоциями
await sendMessage(telegramId, "🎉 Вот твой первый стикер!...", {
  reply_markup: { inline_keyboard: emotionButtons }
});

// Обновить onboarding_step = 1
```

**При `onboarding_step === 1` и `generationType === "emotion"`:**
```typescript
// Стикер с обычными кнопками
await sendSticker(telegramId, stickerBuffer, replyMarkup);

// Показать финальное сообщение
await sendMessage(telegramId, "🔥 Отлично!...", {
  reply_markup: { inline_keyboard: [[buy], [newPhoto]] }
});

// Обновить onboarding_step = 2
```

**При `onboarding_step >= 2` или `99`:**
```typescript
// Обычный режим: кнопки + рейтинг
```

### 5. Логика в index.ts (startGeneration)

**УБРАТЬ:**
- `claim_onboarding_step_1`
- `claim_onboarding_step_2`
- `isOnboardingFree`

**ОСТАВИТЬ:**
- Обычное списание кредитов через `deduct_credits()`

### 6. Диаграмма

```
[Регистрация] → +2 кредита
        ↓
   onboarding_step = 0
        ↓
[Фото → Стиль → Стикер] ← -1 кредит
   Стикер БЕЗ кнопок
   "Добавь эмоцию" + кнопки
        ↓
   onboarding_step = 1
        ↓
   ┌────┴────┐
   ↓         ↓
[Эмоция]  [Пропустить]
-1 кредит  step = 99
   ↓
step = 2
   ↓
[Кредиты = 0, обычный режим]
```

## Чеклист

- [x] Миграция: `onboarding_step` в users
- [x] Существующие пользователи → `onboarding_step = 99`
- [ ] При регистрации: транзакция на 2 кредита
- [ ] Убрать `isOnboardingFree` из startGeneration
- [ ] worker.ts: стикер без кнопок при step=0
- [ ] worker.ts: пропустить рейтинг при step=0,1
- [ ] worker.ts: обновлять onboarding_step после генерации
- [ ] Тестирование
