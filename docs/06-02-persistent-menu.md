# Persistent Menu (Reply Keyboard)

## Цель
Постоянное меню внизу экрана для быстрого доступа к основным действиям.

## UI

```
┌───────────────────────────────────────────────┐
│                 [Чат с ботом]                 │
├───────────────────────────────────────────────┤
│  📷 Фото  │  💰 Баланс  │  ❓ Помощь         │
└───────────────────────────────────────────────┘
```

## Кнопки

| Кнопка | Текст RU | Текст EN | Действие |
|--------|----------|----------|----------|
| Фото | 📷 Фото | 📷 Photo | "Отправь фото — сделаю стикер!" |
| Баланс | 💰 Баланс | 💰 Balance | Меню покупки кредитов |
| Помощь | ❓ Помощь | ❓ Help | Справка |

## Когда показывать

| Момент | Показать меню? |
|--------|----------------|
| `/start` | ✅ Да |
| После генерации стикера | ✅ Да (если ещё не показано) |
| После покупки кредитов | ✅ Да |

---

## Реализация

### 1. Функция меню

```typescript
function getMainMenuKeyboard(lang: string) {
  const texts = lang === "ru" 
    ? ["📷 Фото", "💰 Баланс", "❓ Помощь"]
    : ["📷 Photo", "💰 Balance", "❓ Help"];
  
  return Markup.keyboard([texts]).resize().persistent();
}
```

### 2. Обработка нажатий

```typescript
// Фото
bot.hears(["📷 Фото", "📷 Photo"], async (ctx) => {
  const user = await getUser(ctx.from.id);
  const lang = user?.lang || "en";
  await ctx.reply(await getText(lang, "menu.send_photo"), getMainMenuKeyboard(lang));
});

// Баланс
bot.hears(["💰 Баланс", "💰 Balance"], async (ctx) => {
  const user = await getUser(ctx.from.id);
  if (!user) return;
  await sendBuyCreditsMenu(ctx, user);
});

// Помощь
bot.hears(["❓ Помощь", "❓ Help"], async (ctx) => {
  const user = await getUser(ctx.from.id);
  const lang = user?.lang || "en";
  await ctx.reply(await getText(lang, "menu.help"), getMainMenuKeyboard(lang));
});
```

### 3. Тексты (texts.ts)

```typescript
"menu.send_photo": {
  ru: "Отправь фото — я сделаю из него стикер 🎨",
  en: "Send a photo — I'll make a sticker from it 🎨"
},
"menu.help": {
  ru: "📷 Отправь фото — получи стикер\n💰 Каждый стикер = 1 кредит\n🎨 Выбирай стили и эмоции\n\nВопросы? @p2s_support_bot",
  en: "📷 Send photo — get sticker\n💰 Each sticker = 1 credit\n🎨 Choose styles and emotions\n\nQuestions? @p2s_support_bot"
}
```

### 4. Показ в /start

```typescript
bot.command("start", async (ctx) => {
  // ... существующая логика ...
  
  // Показать приветствие с меню
  await ctx.reply(welcomeText, getMainMenuKeyboard(lang));
});
```

---

## Чеклист

- [x] Функция `getMainMenuKeyboard(lang)`
- [x] Показ меню в `/start`
- [x] `bot.hears()` для "📷 Фото" / "📷 Photo"
- [x] `bot.hears()` для "💰 Баланс" / "💰 Balance"
- [x] `bot.hears()` для "❓ Помощь" / "❓ Help"
- [x] Тексты `menu.send_photo` и `menu.help` в texts.ts
- [ ] Тестирование
